packages:
  - client
  - server

# Autorise la compilation du binding natif (pnpm >= 11 lit ce champ ici ;
# pnpm 10 le lit dans package.json > pnpm.onlyBuiltDependencies).
onlyBuiltDependencies:
  - better-sqlite3
  - esbuild
